bug: drop-down menu inaccessible: found when I clicked an options button of a file in the file tree at the bottom of the screen, and the drop down only showed copy path but no delete, rendering it impossible for me to click that. needs to be screen-aware in that sense so that either the drop down goes up when at the bottom of the screen or the file tree prevents files from going too low based on the size of the drop down.
