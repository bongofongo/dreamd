# dreamd

Lightweight, cross-platform (macOS/Linux) markdown viewer and repo explorer built for a tmux + Neovim + Claude Code workflow. Rust backend (Tauri), file-tree + fuzzy search explorer, CSS-themeable, read-only viewer with a send-selection-to-Claude-Code flow.

Architecture and plan: see project planning doc (Ultraplan).
