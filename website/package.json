{
  "name": "dreamd-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "rm -rf dist && astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
