# Upkeep ledger

Rotation state for the nightly sweep. The area *definitions* — paths, gates,
effort — live in `.claude/skills/upkeep/SKILL.md`; this file holds only which
area is due next and what the last pass found.

The sweep takes the row with the oldest **last swept**, breaking ties by churn
since that date. While **last swept** is empty the `#` column is the order, so
the first cycle runs highest-value-first rather than arbitrarily.

Written by the sweep itself, straight to `main`, whether or not that night
produced code — a PR left unmerged must not stall the rotation. It lives here,
in the public tree, because the job runs in a cloud checkout that has only this
repo: the private `notes/` clone is a local convenience and is not there. This
one-line scheduling record is the **only** thing the nightly job may commit to
`main`; the code half always goes to a PR.

A night that **skipped** rather than swept is the one thing that does not
advance the rotation: it leaves *last swept* where it was and records the skip
in the Outcome, so the area it never touched is still the one due next.

| # | Area | Last swept | Outcome | Open PR |
|---|---|---|---|---|
| 1 | `repo-docs` | 2026-09-06 | swept — six README claims the code had stopped supporting: the mark's paragraph and the print bullet both still called highlights and the stack session state; the perf section said "no CI" after `release.yml`'s `perf-history` job started running the deep tier per release, and listed `deep` at ~15min against ~20min; "both architectures" is three targets, only two of them signed; and the anchoring limit was inverted in both halves — the `line_start > 0` guard exists to keep an unanchorable quote *out* of `Stale`, and `placeAcrossNodes` paints cross-node quotes. `CONTRIBUTING.md` clean. One restatement dropped | #30 (merged) |
| 2 | `shell` | 2026-09-07 | swept — CLAUDE.md said the root has one writer when the sidebar's root field has been the second since `rootfield` landed, and `AppState::repo_root`'s own doc said "only the menu handler" for the same reason; it counted four `Arc`s where `prerender` made five; `main()` still said `--bench-startup` "sits further down, past the walk ... except on a file argument" forty lines above the exit explaining it is deliberately *above* the spawn (the walk has been deferred on every launch since 506fd0e); `cli::run` counted "the other two" against three arms. `send_stack` and `queue_send` held two copies of one rule, now `AppState::chosen_pairs`; `Config::load` moved into the only arm that reads it; a whole `Ui` cloned to read one bool | #31 (merged) |
| 3 | `core-text` | 2026-09-08 | swept — `untrusted`'s envelope sweep spliced the sentinel in three shapes and shape 1 joined its two halves with nothing between them, which is the whole sentinel again: the one arm covering `neutralise`'s reason for substituting rather than deleting was mode 0 spelled twice. Drift: CLAUDE.md's command block listed four bench targets against five (`render_blocks` has been its own since the split, as the same file says forty lines later); `locate_check.rs` still opened with "the repo has no `#[cfg(test)]` tests" against 402; the proptest inventory named six sweeps where there are eight, missing the block-concat identity `render_blocks` stands on; "tier 1 has no scan of its own" is true except past the `u32` cap; `best_match_over`'s doc carried a fragment left by the scan split and a test credited a `best_match` that no longer exists. `guard.rs` clean | #32 (merged) |
| 4 | `store` | 2026-09-16 | swept — `prior` was widened from "read off disk" to "done with", and the widening never reached two of its own sites: `admit`'s inline comment called the flag `#[serde(skip)]` (it is `skip_deserializing`, and the field's doc explains that a plain `skip` would keep the fade from the frontend) and claimed `admit` is the only place it is set, which `mark_sent` and `remove_from_stack` falsified; `doc_from` still said it answers "was this read from a file". `MAX_PER_REPO` was documented as the default for a `marks.max_per_repo` config key that does not exist, deferring to a collection pass that landed as `dreamd marks prune` and deliberately goes through `admit` instead. CLAUDE.md's proptest inventory listed nine sweeps against ten, missing the store's own `retarget_changes_extent_and_nothing_else`; every other store claim in it holds. The FNV-1a constants were spelled out in both `annotations` and `marks_file` — the one duplication the latter's own module doc warns is a silent data-loss bug — and now live once | #33 |
| 5 | `config-theme` | 2026-09-17 | swept — CLAUDE.md called `os_scheme` "the only read" of the OS appearance when the seed in `.setup()` reads through `window_scheme` directly and on purpose, which `main.rs` says in two places; following CLAUDE.md would route startup through a pin-clear that only pins again. `ALIASES`'s "note what is absent" listed three family names against four — `solarized` is absent for the reason the note gives and is the one that reads as an oversight, its light half being listed two lines below it. `prior_fade`'s doc justified its fallback by a caller interpolating the value into a style string, and no such caller exists: the webview paints the fade from the stylesheet's own `var(--hl-prior, 16%)` and the function's only consumers are `theme_check` and the unit tests, which CLAUDE.md never mentioned at all. A test comment cited plan step "D15". `resolve` looked the palette up twice, once to render it and once to name it, reading the user's file off disk both times; `de_zoom` was the fifth clamp spelled out by hand after the sweep that folded the other four, and the per-key function `de_clamped`'s own doc argues against | #34 |
| 6 | `ui-reading` | 2026-09-18 | swept (propose-only) — the byte-identical arm of `writeBackendBlocks` returns without recording the generation the backend just spent, so `lastGen` names a render `main.rs` no longer holds and the *next* save crosses as the whole document: 4.4MB on the corpus doc after every `:w` with no edit, touch or watcher double-fire. Reproduced and fixed in a scratchpad copy of the repo's own harness (437/438 with a probe, 438/438 with three lines). Drift: CLAUDE.md still describes `writeContent` as a `<template>` parse against serialized `outerHTML` and the record as "one serialization off the live DOM" — both are `writeLegacyHtml`, which is the harness stubs, and `lastBlocksBackend` goes unmentioned; `clearHighlights` has one caller and "a file open" is not it; the `querySelectorAll` consumers and the 5,700-line count are the 2026-08-11 drift, still open; staging has no architecture paragraph though `smoke.sh`'s leans on it. Every claim of the three newest paragraphs holds, ui-check's assertions included. Proposed: a widen comment written twice, `wrapRange`'s dead `stale` parameter, a rail cleared twice, the delete path still losing `refreshOutline`/`resetFind`; the 2026-08-11 placer unification downgraded — the `knownText` reuse took its cost | #35 |
| 7 | `agent` | 2026-08-12 | skipped 2026-09-19 through 2026-09-26 — the same three `claude/upkeep-*` PRs open all eight nights (#33 store, #34 config-theme, #35 ui-reading), at the backlog cap every one of them, so this area is still the one due next and has now gone 45 days unswept against a ~14-day rotation. Still no review, comment or push on any of the three beyond this job's own 2026-09-23 flag on #34. Tonight went past `git merge-tree`'s exit status, which is all the previous seven nights reported. The two-dot diffs now read as if all three branches *delete* `theme/contract.rs`, `guide.rs`, `selectors.rs` and `ui/themes/README.md` and *restore* `solarized.css` and the pre-0.4.2 version strings — that is only `main` having moved under them in 7c436e7, 9f2dda3 and 1bc758a, and each branch's own diff against its merge base is small and inside its area (#33 63/48 over `annotations.rs` + `marks_file.rs`, #34 51/44 over `config.rs` + `theme.rs`, #35 the 225-line findings file and nothing else, as propose-only requires). #33 and #35 are safe by construction: `main` has touched none of their files since they were cut. #34 is the only merge crossing a rewrite, and its result is coherent — `canonical` out, `listed_palette` in, `resolve` switched off `css_for`, and `css_for` keeps callers in `main.rs`, `cli.rs`, `theme_check.rs` and one inside `theme.rs`, so the `-D warnings` dead-code failure that overlap invites does not land. Both live findings re-confirmed. **#35's defect is still shipped**: v0.4.2 went out over it and `writeBackendBlocks`' byte-identical arm still returns `[]` without `recordBackend`, so `lastGen` names a generation `main.rs` has replaced and the *next* save crosses as the whole document — 4.4MB on the corpus doc after every no-op `:w`. **#34's stale hunk is one line**: its doc change adds `solarized` to `ALIASES`' "note what is *absent*" list, and 7c436e7 dropped the family outright — `theme.rs` contains the string zero times and `ui/themes/` carries nine — so that single edit is all that stands between #34 and a merge that would drop the backlog to two and release the rotation. Previously swept — `GRANTS` grew 6→7 in 6c56e74 and four prose sites still said six (`gate`'s module doc twice, a stale test cross-reference in `claude`, `agent_spawn`'s doc, and CLAUDE.md); `gate_server::serve_connection` wrapped a `BufReader` it then read straight past with `get_mut()`; a test counter that was never wired to the gate's asker asserted nothing | #21 (merged) |
| 8 | `mcp` | 2026-08-14 | swept — the 1 MiB `MAX_LINE` cap was implemented twice, once per transport, so the reader moved into `jsonrpc` beside the constant; `mcp::fixed_open_doc` had no caller anywhere; `get_open_document` canonicalised the root twice. Drift: a test named for a three-read split that has asserted four since `get_open_document` landed, three references to shipped plan steps, CLAUDE.md saying `register` runs nothing seven lines above describing it running `claude mcp get`, and two different measurements of the same probe in CLAUDE.md and `main.rs` | #22 (merged) |
| 9 | `index-io` | 2026-08-14 | swept — `notify.rs` said "~33 commands" against 54 and "none of them emit" when `set_root` does, via `adopt_root`'s `repo-changed`; CLAUDE.md's list of other unprompted events predated `agent-event`/`agent-ask`; `flow`, `prompt` and `rootfield` had no architecture bullet at all, and the last two pin tenets the coverage sentence omitted. `MarksChanged::anywhere` was dead; the `rel` rule was written twice and now lives once in `fs_walk::rel_of` | #23 (merged) |
| 10 | `ui-panels` | 2026-08-15 | swept (propose-only) — CLAUDE.md's grid, fade, panel-clamp and `WINDOW_TOGGLES` claims all hold exactly. Drift: `#outline-panel`'s markup comment claims it docks left "rather than fighting" the stack panel when it is `right: 8px` and overlaps on purpose; `index.html` names an `installFindCss` that is `findCssOn`/`findCssOff` and calls it a once-per-session install; the settings panel says three tabs where there are four; the print sheet still calls highlights "session state that dies with the process". Proposed: `mark.hl.stale` is unreachable but is `--stale-text`'s only reader, so deleting it is public surface; `WINDOW_TOGGLES` carries two platform flags where one does; `renderBlockPicker` parses the palette twice | #24 (merged) |
| 11 | `os-edges` | 2026-08-16 | swept — CLAUDE.md's tenet 3 quoted the pane's shell as `$SHELL -l -c` when `-i` is the half that finds `claude`; the platform-surface count missed `agent::claude`'s `DEFAULT_SHELL`; tenet 2's "no write outside it" has been false since v1 (`send`'s temp query file, which tenet 3 depends on); `menu.rs` named the wrong submenu as GTK-empty, three lines under its own list saying About is admitted; `main.rs` said four pty commands where `pty_model` made five. `send::tmux_available` spawned a tmux the detector spawned again one line later; the base64 alphabet was written out twice | #25 (merged) |
| 12 | `build-release` | 2026-08-17 | swept — `packaging/SIGNING.md` still opened with "Why this is currently off" and a state table reading `NO_SIGN` set, `TAP_GITHUB_TOKEN` missing and `PUBLISH_CASK` unset, all false since 2026-07-26; its own step 8c is the warning it fell to. `ci.yml` kept a second inventory of the `#[cfg(target_os = "macos")]` sites and named two of five, and called `pull_request` unused when this job's every diff arrives through it; `release.yml` said "both arches" for three targets and omitted the `aur` job; `canary.yml`'s "first five are README's Arch line" were in positions 4–8, and it ran four correctness harnesses where `ci.yml` runs five. `set-version.sh` verified its own write with a grep that also passes when the substitution matched nothing | #26 (merged) |
| 13 | `ui-agent` | 2026-09-03 | swept (propose-only) — CLAUDE.md's pop-out paragraph holds in every part but one sentence, and that one is inverted: the conversation's own rules are written *unscoped* so the moved subtree reads right in either container, and only the differences carry a container prefix; following it as written would break the other container. The vendor injection is the *terminal surface's* first open, not the pane's. Proposed: Escape does not close the docked native pane (no xterm handler exists there and the keydown guard drops it), confirming a permission-mode change never restarts a native session after warning that it would, the send bar's reason string is terminal-only, and `paintModelChips`/`MODE_LABELS` are both second copies | #27 (merged) |
| 14 | `perf-harness` | 2026-09-04 | swept — `perf/README.md` still opened with "No CI, no GitHub Actions" after `release.yml`'s `perf-history` job started running the deep tier per release; `loop.sh` read `events_per_save > 1.0` as "the missing watcher debounce" when `watcher.rs` has had a 60ms window since, so the metric now means the opposite of what its header said; `scroll.mjs` presented `content-visibility: auto` as pending when it was A/B'd and rejected; `run.sh` and the README both refused `--update-baseline` off deep for "reduced sample counts", contradicting the comment saying every tier's settings are identical; four gotchas over five bullets. CLAUDE.md had `publish-history.sh` unwired and recording a machine id it deliberately does not record. `BENCH_ARGS` written identically in all three tier arms folded to one; `Mode::WithContext` constructed nowhere behind an `allow(dead_code)` that named a non-user | #28 (merged) |
| 15 | `ui-style` | 2026-09-05 | swept — `theme.css`'s header split its variables into "required, no fallback" and "optional, has one" and called `--hl-prior` the only member of both; seven of `theme_check`'s twenty-one required variables carry a fallback here and six more never appear in the file at all, so `--hl` and `--font-size` read as optional. The stale-fade comment read as a live invariant when no `mark.hl.stale` reaches the document — all four `wrapRange` call sites pass `stale = false`. `paths.js` named `delete_file` as `inside_root`'s only use. Comments only: the CSS is 162 lean lines and every dedup on offer would have erased a reason | #29 (merged) |

Outcome is one word plus a clause: `clean` (nothing to do), `swept` (a PR
opened), `skipped` (backlog too deep, or blocked — say why), `failed` (a gate
went red and could not be resolved).
